# rules.before
#
# Pre-Routing Rules
#
# These rules are executed before any custom ufw rules.
# If you want to create custom rules, add them to one of these chains:
#   ufw-before-input
#   ufw-before-output
#   ufw-before-forward
#

# Port-forwarding from 8080 to 80
*nat
:PREROUTING ACCEPT [0:0]
-A PREROUTING -p tcp --dport 8080 -j REDIRECT --to-port 80
COMMIT

# Do not remove these essential lines, as they prevent errors
*filter
:ufw-before-input - [0:0]
:ufw-before-output - [0:0]
:ufw-before-forward - [0:0]
:ufw-not-local - [0:0]
# End of essential lines

# Allow all traffic on the loopback interface
-A ufw-before-input -i lo -j ACCEPT
-A ufw-before-output -o lo -j ACCEPT

# Quickly process packets for established connections
-A ufw-before-input -m conntrack --ctstate RELATED,ESTABLISHED -j ACCEPT
-A ufw-before-output -m conntrack --ctstate RELATED,ESTABLISHED -j ACCEPT
-A ufw-before-forward -m conntrack --ctstate RELATED,ESTABLISHED -j ACCEPT

# Drop packets with invalid connection states (logged in medium and higher log levels)
-A ufw-before-input -m conntrack --ctstate INVALID -j ufw-logging-deny
-A ufw-before-input -m conntrack --ctstate INVALID -j DROP

# Accept ICMP packets with specific types for INPUT
-A ufw-before-input -p icmp --icmp-type destination-unreachable -j ACCEPT
-A ufw-before-input -p icmp --icmp-type source-quench -j ACCEPT
-A ufw-before-input -p icmp --icmp-type time-exceeded -j ACCEPT
-A ufw-before-input -p icmp --icmp-type parameter-problem -j ACCEPT
-A ufw-before-input -p icmp --icmp-type echo-request -j ACCEPT

# Accept ICMP packets with specific types for FORWARD
-A ufw-before-forward -p icmp --icmp-type destination-unreachable -j ACCEPT
-A ufw-before-forward -p icmp --icmp-type source-quench -j ACCEPT
-A ufw-before-forward -p icmp --icmp-type time-exceeded -j ACCEPT
-A ufw-before-forward -p icmp --icmp-type parameter-problem -j ACCEPT
-A ufw-before-forward -p icmp --icmp-type echo-request -j ACCEPT

# Allow DHCP client traffic to function
-A ufw-before-input -p udp --sport 67 --dport 68 -j ACCEPT

#
# Handle non-local packets in ufw-not-local
#
-A ufw-before-input -j ufw-not-local

# If the destination address is LOCAL, RETURN
-A ufw-not-local -m addrtype --dst-type LOCAL -j RETURN

# If the destination address is MULTICAST, RETURN
-A ufw-not-local -m addrtype --dst-type MULTICAST -j RETURN

# If the destination address is BROADCAST, RETURN
-A ufw-not-local -m addrtype --dst-type BROADCAST -j RETURN

# Drop all other non-local packets with rate limiting and logging
-A ufw-not-local -m limit --limit 3/min --limit-burst 10 -j ufw-logging-deny
-A ufw-not-local -j DROP

# Allow MULTICAST mDNS for service discovery (ensure the MULTICAST line above is uncommented)
-A ufw-before-input -p udp -d 3.90.85.41 --dport 5353 -j ACCEPT

# Allow MULTICAST UPnP for service discovery (ensure the MULTICAST line above is uncommented)
-A ufw-before-input -p udp -d 54.174.187.4 --dport 1900 -j ACCEPT

# Do not delete the 'COMMIT' line, as it is necessary for rule processing
COMMIT
